# VCS
.git
.gitignore
.gitattributes
.github/

# IDE / editor
.vscode/
.idea/
*.code-workspace
*.swp
*.swo

# Local env / secrets (never needed to build the binary)
.env
.env.*
*.local

# Build artifacts / test outputs
bin/
dist/
tmp/
*.test
coverage.out
govulncheck.txt

# Logs
*.log

# Dependencies (only if you don't vendor in-image)
# If you ever switch to vendoring in-image, remove this ignore.
vendor/

# OS junk
.DS_Store
Thumbs.db

# Optional: docs not required for build context
README.md
docs/
